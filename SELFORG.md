# NEURONS 自己組織化システム

ボードゲーム **NEURONS** のピースとルールを、そのまま力学系として走らせる試み。
盤面を興奮性媒質として時間発展させると、進行波・スパイラル波・時空的な乱流が生まれる。
以下では相の呼び名として「沸騰」を使うが、これは**波面の切断と再生成が定常的に続く状態**の略称であって、
比喩ではない(§2.7)。

- 実装: [`neurons_selforg.html`](neurons_selforg.html) — 単一HTML、依存ゼロ、日英切替
- エンジンと検証ツール: [`selforg_engine/`](selforg_engine/)
- 代表的な状態の動画: [`selforg_movies/`](selforg_movies/)

---

## 1. ゲームをどう拡張したか

### 1.1 ゲームから、そのまま持ってきたもの

| ゲームの要素 | 力学系での役割 |
|---|---|
| 4種のカードの線分 | 結合グラフの辺 |
| 線はカードの**角で出入りする** | 辺は必ず「隅ノード ↔ 中心ノード」を結ぶ(二部グラフ) |
| カードの**丸(ニューロン)** | 中心ノード = 発火する素子 |
| 丸の**色**(白 / 黒) | 出力の符号(後述の Dale 則) |
| **多数派の色が回路を取る** | 静止ノードの発火判定 |
| **同数なら不成立** | 正味入力が 0 のとき何も起きない |
| 閉じた多角形 = **サーキットの発火** | リエントラントループ = 自励振動源 |
| 周上の丸 + 内部の丸のみカードで**得点** | 面ごとの興奮性の場(パラメータ `g`) |
| **辺または角に接する所にしか置けない** | 配線の成長則(戦略性 `S` を通じて効く) |
| デッキ構成 8:8:4:4 | 次数分布 → 分岐率 2.5(§2.3) |
| 置く前の回転は自由 | 向きの一様ランダム化 |

面(サーキット)の列挙は、ゲーム本体 `index.html` と**同一のアルゴリズム**(角度ソートによる半辺トレース)を使っている。
辺数・面数・面積・面の色収支が 30 通りの盤面構成で完全に一致することを検証済み(`selforg_engine/test_faces.js`)。

### 1.2 拡張として**足した**もの — 3つだけ

**(a) 時間 — Greenberg–Hastings 型の興奮性オートマトン。**
各ノードに `静止 / 興奮 / 不応(R歩)` の状態を与え、同期更新する。これは
**Greenberg–Hastings 模型**(Greenberg & Hastings 1978)そのもので、興奮性媒質を離散化した
最も単純なモデルとして知られている。本システムはこれを土台に、次の 3 点で変更している:

1. **入力に符号がある**(下の (b))。元の模型は興奮している近傍の**数**だけを見る。
2. **正則格子ではなく二部グラフの上に載る**。ノードは中心と隅の 2 種で、辺は必ず両者を結ぶ。
   §2.5 のパリティ則はこの構造から出る。
3. **閾値が場所ごとに違う**。ゲームの得点計算から作った凹凸 `g·faceMod` を閾値から引く(§2.8)。

静止ノードは、興奮している隣接ノードの**符号和 s** を見て:

- **中心ノード(ニューロン)**: `s ≥ θ − g·faceMod` なら興奮。
- **隅ノード(合流点)**: `s > 0` なら +、`s < 0` なら −、**`s = 0` なら何も起きない**。

最後の一行が、ゲームの「同数なら不成立」の直訳になっている。

**(b) 色の意味づけ(Dale 則)。** 白い丸は発火すると **+1** を、黒い丸は **−1** を出す。
自分の色が自分の出力符号を決め、それは入力によらない。これは神経科学の Dale 則そのもので、
「多数派の色が回路を支配する」というゲームの勝敗規則が、そのまま興奮性/抑制性のバランスになる。

**(c) 回路の帰還(`faceA`)。** 得点ルールを力学に閉じる:
**ある回路の周のノードが `faceWin` 歩以内に全部発火したら、その回路は「発火した」とみなし、
周のノードを `faceDur` 歩のあいだ沈黙させる**(閾値を `faceA` だけ上げる)。
同じ回路が再び発火できるようになるまでには `faceRef` 歩の不応期を置く。

**この (c) が、系にカオスをもたらす唯一の要素である**(§2.7)。(a) と (b) だけの系は、
後述するとおり厳密に周期的である。

### 1.3 捨てたもの・効かなかったもの

勝敗、手番、山札の有限性、得点の累計は力学には使っていない(表示はする)。

より重要なのは、**足しても何も起きなかった**もので、いずれも実測で棄却した:

| 試したこと | 調べた点数 | 結果 |
|---|---|---|
| 使用依存の疲労(発火するほど不応期が延びる) | 346 | カオスを生まない。回復時間が短いほど次の不応期が**延びる**ので、波の破断に必要な復元関係と符号が逆(§2.7) |
| 抑制の強さ `w_I` を連続量にする | — | 力学的に不活性。入力は整数なので `sign(1−w_I)` しか効かない |
| 抑制性ニューロンの不応期 `R_I` を変える | 290 | 抑制性は伝導しないので、準静的な veto 場を定数倍するだけ |
| 非同期更新(ロックステップを崩す) | 279 | 周期性の原因ではなかった。完全逐次更新でも厳密周期が残る |
| シャント型(除算的)抑制 | 366 | 摂動に敏感にはなるがカオスにはならない |
| 不均一な伝導遅延 | 253 | 摂動が凍った傷として残るだけで、傷の大きさが系のサイズに依らない |
| 幾何学的な無秩序(被覆率、壁/トーラス、希釈) | 257 | すべて反カオス方向。壁とトーラスは全統計で区別不能 |

合計 **2,880 点**のパラメータ点を 11 領域で走査した結果、
**元のルールの範囲内にカオス相は存在しない**ことが確定している。

---

## 2. できたシステムの物理

### 2.1 何であるか

**二部格子上の、符号付き興奮性媒質**である。

ノードは 2 種類しかない。**中心**(カードの丸 = ニューロン)と**隅**(線の合流点)で、
辺は必ずこの 2 種類のあいだを結ぶ。したがって活動は `中心 → 隅 → 中心` と交互に進み、
**1 歩で波面は半マス進む**。

### 2.2 抑制性ニューロンは、なぜ必要か

黒いニューロンは発火しても −1 を出すので、下流の隅は負になり、その先の中心は閾値を超えられない。
つまり**抑制性ニューロンは興奮を伝えない**(実際の脳でもそうである)。活動は白いニューロンの
骨格の上だけを走り、黒は「経路を塞ぎ、隅で正の信号を打ち消す」障害物として働く。

ところが、**抑制を完全になくすと系は死ぬ**。白黒比 `q = 1.0` ちょうどでは、どんな配線でも活動が
絶える。波が綺麗に伝わって互いに衝突・対消滅し、それで終わってしまうからだ。`q = 0.99` なら
頑健に生き残る。**持続には波が破れて自由端ができることが要り、それを起こしているのが抑制性
ニューロンである。** 抑制は邪魔者ではなく、媒質を生かしている当のものだ。

### 2.3 生きるか死ぬか — パーコレーション

デッキ構成から、配線の連結性は解析的に見積もれる。

- 中心ノードの次数: diag/vee → 2、cross → 4、dot → 0。⟨k⟩ = 2.000、⟨k²⟩ = 5.333、**超過次数 5/3**
- 隅ノードは隣接 4 セルが各々確率 1/2 で辺を出すので次数 ~ Bin(4, 1/2)。⟨k⟩ = 2.000、**超過次数 3/2**
- 分岐率(中心 → 隅 → 中心)= (3/2) × (5/3) = **2.5**

白のみが伝導するので実効分岐率は **2.5 q**、木近似では臨界 `q_c = 0.4`。
しかし**実測は q_c ≈ 0.6〜0.7**(印刷デッキ)。ループと不応期による阻害の分、木近似は楽観的だった。
**ゲーム忠実の q = 0.5 は臨界のすぐ下**に位置する — つまり白黒が拮抗した盤を適当に埋めると沈黙する。

### 2.4 実効次元 — `cross` カードが媒質を2次元にする

平均次数が中心も隅も 2.0 ということは、印刷デッキの配線は**長い紐が絡まったネットワーク**に近い。
静止した媒質に 1 点刺激を入れると、波面は円状に広がろうとするが**断片化する**。

`cross`(次数 4)カードの比率を上げると媒質は 2 次元シートに近づく:

| cross 比率 | 波面 |
|---|---|
| 1/6(印刷デッキ) | 断片化した、ちぎれた輪 |
| 0.5 以上 | 連続した波面 |
| 1.0 | 完全な正方形の波面(格子の L∞ 距離を反映) |

**デッキ配合が媒質の実効次元を制御している。**

### 2.5 波長とパリティ則

1 歩で半マス進み、1 回の興奮 + 不応サイクルは `R + 1` 歩なので、**波長 ≈ (R+1)/2 マス**。
R = 20 なら約 10.5 マスになる。

そして二部性から**厳密なパリティ則**が出る:

> **`R = 2k−1` と `R = 2k` は力学的に完全に同一である。**

中心と隅が交互に更新されるので、奇数オフセットで回復したノードには入力が来ない。
実効不応期は `2·ceil(R/2)` になる。**奇数の R は目盛りとして無意味**なので、UI は偶数刻みにしてある。

### 2.6 相の分類 — 見た目は乱流でも、厳密に周期的

これがこの系のいちばん重要な性質である。

乱流にしか見えない状態でも、**状態を厳密に比較すると短い周期に落ちている**(典型的には周期 6〜24、
概ね `R + 2`)。全ノードが不応期の許す最速で発火し続ける飽和状態だ。
総当たりの状態比較で検証済み(`selforg_engine/test_period.js`)。

さらに、盤を大きくすると**全体周期が超指数的に伸びる**ように見える(60×40 で 40、170×110 で 25,080、
260×170 では 30,000 歩以内に見つからない)。これはクラス3(カオス)の典型的な兆候に見えるが、
**完全に見かけである**:

> **アトラクタは独立な局所周期の直積であり、全体周期はその最小公倍数にすぎない。**

活動は空間的に分離したドメインに分裂し、各ドメインの周期は小さい(8〜460)。
260×170 の盤について分解から周期 **38,640** と **171,360** を事前に予測し、
25 万歩の厳密探索が**まさにその値**を返した。過渡期は 60〜800 歩と短く、系は約 200 歩で
厳密な周期に落ちてから、あとは長い距離計を回しているだけである。教科書的なクラス 2 だ。

秩序盤の全体周期は 6.1×10⁸ にも達する。**「周期が見つからない」は真実だが誤った印象を与える**
ので、UI は全体周期を表示しない(§3.6)。

### 2.7 カオスはどこから来るか — 回路の帰還

`faceA` を入れると相が変わる。**同一の配線で、スイッチ 1 つだけの違い**:

| 回路の帰還 | 1ノード摂動の成長 | 周期 |
|---|---|---|
| `faceA = 0` | **0 / 18** | **厳密に 22** |
| `faceA = 2` | **6 / 18** | 検出されず |

摂動が成長するときは、生ノードの **40〜70%** に伝染して定着する。

#### 何が起きているか

1. ある閉回路の周ノードが `faceWin` 歩以内に全部興奮する。これは**波がその回路を一周した**ことを意味する。
2. その瞬間、**周ノード全部の発火閾値が同時に `faceA` だけ上がり、`faceDur` 歩そのままになる**。
3. 閾値の上がった領域には、他所から来た波面が入れない。入ろうとした波面はそこで局所的に止まり、**切断される**。
4. 切断された波面には自由端ができ、自由端は回り込んで新しい回転核になる。
5. 新しい回転核は別の回路を一周させ、その回路の周を沈黙させる。以下同様。

つまり**閾値の凹凸が活動自身によって作られ、その凹凸が次の活動の配置を決める**。
波面と閾値場が互いに相手を決めるので、どちらも定常配置に落ち着けない。
単一ノードの違いは「どの回路が先に一周するか」を変え、それが沈黙領域の位置を変え、
そこから波面の切れ方が変わる。これが摂動の増幅される経路である。

#### なぜ「ニューロン1個の疲労」では同じことが起きないか

発火するほど**そのニューロン自身の**不応期が延びる、という機構(346 点走査済み)は、
一見似ているが 2 つの点で決定的に違う。

**(i) 空間的にばらばらである。** 各ノードの余分な不応は自分の最後のスパイクを基準に決まるので、
抑制されている領域は**モザイク状**になる。波面はその隙間を縫って通ってしまい、切断されない。
回路の帰還では、一周した回路の周が**同時に、同じ長さだけ**沈黙する。抑制領域が回路の大きさを持つ
連続した障害物になるので、他の波面を切断できる。

**(ii) 復元関係の符号が逆である。** 波の破断が起きるためには、
「直前の回復時間が短いほど、次の不応期も短くなる」という関係(復元曲線の傾きが 1 を超える状態)が要る。
ところがニューロン単位の疲労では、回復時間が短いと疲労変数が高く残るので、**次の不応期はむしろ延びる**。
これは破断に対して最大限に安定化する向きで、実際にどのパラメータでも摂動は完全に治癒した。

**測定したのは (1)〜(5) の結果と、疲労が効かないという事実である。**
(i)(ii) はその 2 つを説明する物理的な議論であって、独立に測定したものではない。

### 2.8 得点ルールが「場」になる — `g`

各面について「周上の丸 + 内部の丸のみカード」の白黒収支を計算し、その面の周ノードの閾値を
上下させる。ゲームの得点計算がそのまま**焼き込まれた興奮性の凹凸**になる。

これは飾りではない。**閾値 θ = 2(同時到着を要求する)では `g = 0` だと媒質は死ぬが、
ある鋭い `g_c` を超えると蘇る。** 得点ルールが作る「通りやすい経路」の網が浸透した瞬間に伝導が始まる。

### 2.9 打ち手の戦略が配線を変える — `S`

`S = 0` は一様ランダムな合法手、`S = 1` は Union-Find で「回路を閉じ、自色が多数派になる手」を
探す貪欲な打ち手。**打ち手が賢くなると回路構造そのものが変わる**:

| | 辺 | 回路 |
|---|---|---|
| 13×13, S=0 | 341 | 36 |
| 13×13, S=1 | 459 | **120** |

そして印刷デッキ・白黒拮抗(q = 0.5)の盤で、これが生死を分ける(§3.4 の第1幕)。

---

## 3. 使い方

### 3.1 開く

```
open neurons_selforg.html
```

ブラウザで開くだけ。サーバも、ビルドも、外部アセットも要らない。
言語はブラウザ設定に従い、ヘッダの `EN` / `日本語` ボタンで切り替わる。

### 3.2 画面の見方

盤面は全画面。既定は**紙の配色**(クリーム地に、白ニューロンの発火 = 朱、黒ニューロンの発火 = 青、
不応期 = 褪せた色)。「表示: 発光」で暗背景のネオン表示に切り替わる — 投影ではこちらの方が
コントラストが出る。**配色はズームでは変わらない**。

- **クリック** — そこから活動が広がる
- **ホイール** — 拡大縮小。**セル 13px を境に、遠景の高速描画から実際のカードの手描き表示に切り替わる**
- **ドラッグ** — 移動(盤の外へは出られない)

右上の読み取り:

| 表示 | 意味 |
|---|---|
| **活動** | いま興奮しているノード数 |
| **位相ロック** | スパイク列が局所周期に乗っているノードの割合(§3.6) |
| **秩序 / カオス** | 双子宇宙との差分から判定 |
| **発火した回路** | 一周した回路の累計 |

### 3.3 主ノブ

**これ 1 本で相図の上を歩く。** 実測で単調性を確認した経路になっている
(単純に白黒比を上げる経路は、進行波より先に破断状態を通ってしまうので使えない)。

| u | 相 | 位相ロック |
|---|---|---|
| 0.05 | 沈黙 | — |
| 0.15 | まばら — 数少ない導通経路を這う一つの火花 | 98% |
| 0.38 | 進行波 | 100% |
| **0.55** | **スパイラル** — 厳密に周期 22。話し手が留まるならここ | 100% |
| 0.68 | 波面の破断が始まる | 63% |
| 0.90 | **一周した回路が周を沈黙させ、その沈黙領域が次の波面を切断する。切断と再生成が続き、状態は二度と同じ配置に戻らない** | 3% |

前半は白黒比を高く保ったまま**得点の場 `g` を上げ**、後半で `g` を補償しながら白黒比を下げて
波を破る障害物を増やし、最後に**回路の帰還**を噛ませる。
`u = 0.48〜0.64` は意図的に平坦にしてある(安定したスパイラルの高原)。

**盤は作り直されない。** 白黒比・得点の場・回路の帰還はいずれも配線の幾何を変えないので、
ノブを回しても同じ盤の上で状態が連続的に変化する。

### 3.4 第1幕 — 印刷されたデッキ

白黒 1:1 の印刷デッキをランダムに埋めると、興奮性ニューロンの骨格が浸透しない(§2.3)ので活動は伝わらず、
数十歩で消える。**同じデッキでも、回路を狙って打つと配線の連結性が上がり、
得点ルールを場として使うと閾値が下がるため、活動が盤面規模で持続するようになる。**
すべて `cross = dot = 1/6`、白黒 1:1 の**忠実な**デッキである。

| プリセット | 定常活動 | 回路数 |
|---|---|---|
| 沈黙(ランダムに埋める) | **0** | 4,044 |
| 下手な打ち手 (S=0.8) | 82 | 12,205 |
| 本気で打つ (S=1) | 382 | 13,331 |
| 得点の場 (g=2) | 606 | 4,044 |
| 両方 | **1,528** | 13,331 |

8 シード中 8 つで再現する。「得点の場」は**沈黙と全く同じ配線**で、読み方を変えただけである点に注意。

### 3.5 第2幕 — 交差の多いデッキ

主ノブの停留点そのもの。ボタンはノブを所定の位置に飛ばす。

**「回路の帰還」ボタン**は独立している。スパイラルが回っている状態で押すと、
配線も状態も保ったまま `faceA` が 0 ↔ 2 に切り替わる。**これが実演の核**で、
走っているスパイラルから入ると必ず成功する(ランダムな種火からは点火に失敗しやすい)。

### 3.6 秩序とカオスをどう示すか

**位相ロック率(常時表示)。** 数千ノードのスパイク列の直近 256 歩を保持し、
KMP 失敗関数で各ノードの最小周期を求め、ロックしている割合を出す。
全体周期を検出する方式は採らなかった — 1 ステップの 2.1〜2.5 倍のコストがかかる上に、
§2.6 の最小公倍数構造のせいで**秩序相を 31〜69% の確率で「非周期」と誤判定する**からだ。
局所周期なら 0.43 ms/フレームで、ランプではなく**ダイヤル**として機能する。

**ダメージ拡散(「差分を見る」)。** 同じ盤面・同じ状態の宇宙をもう一つ用意し、
**1 ノードだけ**状態を変えて並走させる。差のあるところがマゼンタで描かれる。
秩序相では差が消え、カオス相では全域に広がる — カオスの定義そのものである。

単発では約半分が治癒するので、**治癒したら自動で打ち直し、累計を表示する**。
「治癒 7 ・ 伝染 5」という比率こそが主張の中身であって、1 回の成功ではない。

### 3.7 速度

主ノブのすぐ下。既定は**自動**で、実測の ms/歩 から、そのマシンが維持できる最速を選ぶ
(サーマルスロットリングにも追従)。スライダーを触ると手動に切り替わり、「速度: 自動」で戻る。

目盛りは等比で **1/30 歩/フレーム 〜 24 歩/フレーム の 18 段**。
1 歩は波面が半マス進む量なので、最低速では**カードが 1 枚ずつ光るのを目で追える**。
横に「2 歩/フレーム · 0.54 ms/歩 · 58 fps」と実測値が出る。

### 3.8 パラメータ(詳細パネル)

**効くもの**だけを置いてある。実測で不活性と分かったもの(抑制の強さ、抑制性の不応期、疲労)は
意図的に外した。

| 層 | パラメータ | 備考 |
|---|---|---|
| 力学(即時) | `R` 不応期 | **偶数刻み**(§2.5)。波長 ≈ (R+1)/2 マス |
| | `faceA` 回路の沈黙 | 実質スイッチ。0 か 2 |
| | `faceDur` 沈黙の長さ | こちらが連続的な強度 |
| | `g` 得点の場 | 面の色収支を興奮性に反映 |
| 配線(再生成) | `q` 白:黒 比 | 生死の主変数。`q_c ≈ 0.6`、`q = 1.0` は死 |
| | `cross` 交差カード | 媒質の実効次元 |
| | `dot` 丸のみカード | 配線の希釈 |
| | `S` 打ち手の戦略性 | 0 = ランダム、1 = 貪欲に回路を狙う |
| | `p` 被覆率 | |
| | マスの大きさ | 盤の大きさが窓に合わせて決まる |

### 3.9 キーボード

| キー | 動作 |
|---|---|
| `space` | 再生 / 停止 |
| `←` `→` | 主ノブ |
| `,` `.` | 速度を一段ずつ |
| `1`–`5` | 第1幕のプリセット |
| `6`–`0` | 第2幕のプリセット |
| `F` | 回路の帰還 |
| `D` | 差分表示 / `N` 打ち直し |
| `R` | 配線を作り直す |
| `K` | 自動巡回 / `L` 言語 / `?` ヘルプ |

### 3.10 展示運用

「自動巡回」でプリセットを順に回る無人モードになる。
**活動が 30 歩ゼロになると自動で再点火する**ので、放置しても黒画面で止まらない。

投影では「表示: 発光」を推奨。可読性の実測では、**セル 4px 未満で相の判別がつかなくなり、
6px 前後が最良**、12px では 1 セルの粒がパターンと同じ大きさになって粗く見える。
画面幅あたり 240〜480 マスに収めるとよい。

---

## 4. 下敷きにしたモデルと用語

このシステムは既存のモデルの組み合わせで、新しく作ったのは 1 箇所だけである。

| 名前 | どこで使っているか |
|---|---|
| **Greenberg–Hastings 模型** (Greenberg & Hastings 1978) | 力学の核。`静止 / 興奮 / 不応` の 3 状態興奮性オートマトン。上記 3 点を変更して使用 |
| **Dale 則** | ニューロンの出力符号が自分の型で決まり、入力によらないこと。ゲームの「丸の色」がそのままこれになる(§1.2b) |
| **興奮性媒質のリエントラント波・スパイラル波** | 閉回路が自励振動源になる仕組み。心臓電気生理と反応拡散系の標準的な枠組み(Winfree ら) |
| **復元関係(restitution)と傾き 1 の判定** | 波面の破断が起きる条件。心臓電気生理由来。疲労が効かない理由の説明に使用(§2.7) |
| **ダメージ拡散** (Derrida & Weisbuch 1986) | カオスの判定法。同一状態の双子を作り 1 ノードだけ変えて並走させる。UI の「差分を見る」そのもの |
| **Wolfram のクラス分類** (Wolfram 1984) | クラス 2(周期)とクラス 3(カオス)の区別。§2.6 の結論はクラス 2 |
| **パーコレーションと分岐率**(Molloy–Reed 型の判定) | 配線が導通するかどうかの見積もり。超過次数の積で臨界を評価(§2.3) |
| **Eden 成長** (Eden 1961) | 種から接触ルールで広げる配置。被覆率 `p < 1` のときの塊の形 |
| **漏れ積分発火(LIF)** | 別のカオス経路として検証した変種(§6) |
| **スパイク頻度適応(使用依存の疲労)** | 検証して**棄却**した機構(§1.3, §2.7) |
| **KMP 失敗関数** | 位相ロック率の測定に使うアルゴリズム(§3.6) |
| **Union-Find** | 打ち手が「この手で回路が閉じるか」を判定する前処理(§2.9) |

**標準的な名前がないのは「回路の帰還」(§1.2c)だけである。**
グラフの閉路そのものに載る遅い抑制変数で、閉路が一周したことを検知して周を沈黙させる。
機能としては心臓のリエントラントにおける機能的ブロックに近いが、
「面の一周を検出して面全体を抑制する」という形の標準モデルは見当たらなかった。
ゲームの得点ルール(回路が閉じたら発火して勝敗が決まる)を力学に閉じた結果として出てきたものである。

> 出典の年号は記憶によるもので、公表前には確認されたい。

---

## 5. 再現とツール

```
selforg_engine/
├── engine.js              配線生成・面列挙・力学・PNG 書き出し
├── explore.js             パラメータ走査と相判定のハーネス
├── render_all.js          動画の生成(両配色)
├── test_faces.js          面列挙をゲーム本体 index.html と突き合わせる
├── test_port.js           HTML 内のエンジンが検証済みエンジンと同一か確認する
├── test_period.js         周期検出器を総当たりの状態比較で検証する
├── var_faceback/          回路帰還版エンジン
└── var_lif/               漏れ積分発火版エンジン(別のカオス経路)
```

```
node test_faces.js        # 幾何がゲーム本体と一致するか
node test_port.js         # 移植が数値的に同一か
node render_all.js        # 動画を作り直す(約2分)
node explore.js '{"base":{"cross":0.6,"q":0.8,"R":20},"grid":{"faceA":[0,2]},"seeds":[1,2,3]}'
```

すべて決定論的で、依存パッケージはない。

---

## 6. 既知の限界

**カオスは「非周期的で摂動を増幅する」状態であって、一様に強いカオスではない。**
単発の摂動が成長する確率は 1/3〜1/2 で、残りは治癒する。累計で示すべき性質である。

**カオス相は印刷デッキでは出ない。** `cross ≈ 0.6` が要るのに、実際のデッキは `1/6` である。
第2幕がデッキ構成を変えているのはこのためで、第1幕(忠実デッキ)とは別の話として扱っている。
言い換えれば、**このデッキは良いゲームになるよう調整されていて、カオスを起こすようには出来ていない**。

**もう一つのカオス経路がある。** 離散的なニューロンを漏れのある膜電位(積分発火)に置き換えると、
回路の帰還なしでもカオスが出る(`cross=0.75, q=0.9, R=4, θ=1.5, leak=0.95`)。
生存条件 `leak² ≥ θ−1`、カオス条件 `leak⁴ ≥ θ−1` という 2 本の線が解析的に出る。
ただし不応期が R ∈ {3,4} に限定されるため波長が約 3 マスしかなく、模様として読めない。
展示には採らなかったが `selforg_engine/var_lif/` に残してある。

**カオス相には空間的な周期構造がない。** 秩序相のスパイラルは相関に明瞭な二次ピーク
(波長 12 マス)を持つが、カオス相にはそれがなく、相関は 1〜2 マスで切れる。
画面上は数マス規模の塊が生成・消滅を繰り返す組織に見える。
R を大きく取れば塊が 5〜10 マスに育って読めるようになる。

---

Game design: Eiji Watanabe / Implementation: Claude (Anthropic) / All Rights Reserved 2026-
